apiVersion: v1
kind: Service
metadata:
  name: {{ include "novalume-catalog-service.fullname" . }}
  labels:
    {{- include "novalume-catalog-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "novalume-catalog-service.selectorLabels" . | nindent 4 }}
